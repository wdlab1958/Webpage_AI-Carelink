// Tailwind CSS v4 — 반응형(md:/lg:) · 임의값 등 유틸리티 제공.
// globals.css 의 수작업 유틸리티는 @layer components 로 두어 Tailwind utilities 가 우선한다.
const config = { plugins: { '@tailwindcss/postcss': {} } };
export default config;
